(executable
 (name osat)
 (libraries unix dolmen)
 (modules osat bexp solver solver_bt))

(executable
 (name osat2)
 (libraries unix dolmen)
 (modules osat2 bexp2))

(env
  (dev
    (flags (:standard -warn-error -A))))